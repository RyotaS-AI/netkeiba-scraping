# 競馬スクレイピング 自動化手順書

## 概要

GitHub Actions を使い、PC を閉じた状態でも自動でスクレイピングを実行し、結果を Google Drive に保存する仕組み。

---

## システム構成

```
race_ids.txt にrace_idを記入してpush
        ↓
GitHub Actions が自動起動（cron または手動）
        ↓
main.py がスクレイピングを実行
        ↓
Google Drive にCSVを保存
```

---

## 初期設定（一度だけ実施）

### 1. Google Cloud の設定

1. [Google Cloud Console](https://console.cloud.google.com/) でプロジェクトを作成
2. **Google Drive API** を有効化（APIとサービス → ライブラリ → Google Drive API）
3. **OAuth同意画面** を設定（APIとサービス → OAuth同意画面）
   - User Type: 外部
   - アプリ名・メールアドレスを入力
   - 対象 → テストユーザーに自分のGmailアドレスを追加
4. **OAuth クライアントID** を作成（APIとサービス → 認証情報 → 認証情報を作成）
   - 種類: デスクトップアプリ
   - JSONをダウンロードして `credentials.json` にリネームし、プロジェクトフォルダに配置

### 2. リフレッシュトークンの取得（ローカルで一度だけ実行）

```bash
pip3 install google-auth-oauthlib
python3 get_refresh_token.py
```

ブラウザが開くので自分のGoogleアカウントでログイン。
ターミナルに表示される3つの値をメモする。

```
GOOGLE_OAUTH_CLIENT_ID:     xxx
GOOGLE_OAUTH_CLIENT_SECRET: xxx
GOOGLE_OAUTH_REFRESH_TOKEN: xxx
```

### 3. Google Drive の設定

1. Google Drive で出力先フォルダを作成
2. フォルダのURLからIDをメモ（`https://drive.google.com/drive/folders/【ここ】`）

### 4. GitHub リポジトリの作成

```bash
cd /Users/hanekujira/Documents/dev/netkeiba_scraping
git init
git branch -m main
git add .
git commit -m "initial commit"
git remote add origin https://github.com/hanekujira/netkeiba-scraping.git
git push -u origin main
```

### 5. GitHub Secrets の登録

リポジトリの Settings → Secrets and variables → Actions で以下を登録：

| Secret名 | 値 |
|---|---|
| `NETKEIBA_LOGIN_ID` | netkeibaのログインID |
| `NETKEIBA_PASSWORD` | netkeibaのパスワード |
| `GOOGLE_OAUTH_CLIENT_ID` | 手順2で取得した値 |
| `GOOGLE_OAUTH_CLIENT_SECRET` | 手順2で取得した値 |
| `GOOGLE_OAUTH_REFRESH_TOKEN` | 手順2で取得した値 |
| `GOOGLE_DRIVE_FOLDER_ID` | 手順3でメモしたフォルダID |

---

## 毎週の使い方

### スケジュール実行（自動）

1. `race_ids.txt` に当週のrace_idを記入して保存

```
# 例
202506010811
202506010812
```

2. GitHubにpush

```bash
git add race_ids.txt
git commit -m "race_ids更新"
git push
```

3. 設定した時刻に自動実行され、Google Drive にCSVが保存される

### 手動実行

GitHub → Actions → 競馬スクレイピング → **Run workflow** → race_id（12桁）を入力 → 実行

---

## スケジュールの変更

`.github/workflows/scrape.yml` の cron 行を編集する。

```yaml
- cron: '0 22 * * 5,6'  # 毎週土・日曜 7:00 AM JST
```

cron の書式: `分 時(UTC) 日 月 曜日(0=日,5=金,6=土)`

JST → UTC の変換: JST - 9時間（例: 朝7時JST = 前日22時UTC）

---

## ファイル構成

```
netkeiba_scraping/
├── main.py                  # メイン処理
├── scraper.py               # HTTPスクレイパー
├── exporter.py              # CSV出力 + Google Driveアップロード
├── config.py                # 環境変数の読み込み
├── race_ids.txt             # 実行対象のrace_idリスト
├── get_refresh_token.py     # リフレッシュトークン取得（初回のみ）
├── requirements.txt         # Pythonパッケージ一覧
├── .github/workflows/
│   └── scrape.yml           # GitHub Actionsワークフロー
└── docs/
    ├── 仕様書.md
    └── 自動化手順書.md（本ファイル）
```
